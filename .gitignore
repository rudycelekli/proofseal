# Dependencies
node_modules/

# Build output (rebuilt on publish via prepublishOnly)
/dist/

# Agent-orchestration / local tooling artifacts — never ship these
.swarm/
.claude-flow/
*.db
*.db-wal
*.db-shm
ruvector.db

# Local env / logs / OS cruft
.env
.env.*
*.log
coverage/
.DS_Store
